<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================================ -->
    <!--  SHARED PARTS                                                -->
    <!-- ============================================================ -->

    <!-- Filled (value) portion of the default cyan slider -->
    <Style x:Key="Slider.FillButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Height="4" CornerRadius="2" VerticalAlignment="Center"
                            Background="{DynamicResource Brush.Accent}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Filled portion for axis sliders: full-strength axis color (from Foreground) -->
    <Style x:Key="Slider.AxisFillButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Height="4" CornerRadius="2" VerticalAlignment="Center"
                            Background="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Remaining (unfilled) portion: invisible but click/drag-able -->
    <Style x:Key="Slider.EmptyButton" TargetType="RepeatButton">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 15px white knob; ring color = owning Slider's Foreground.
         Hover/drag scales the knob (works for any ring color). -->
    <Style x:Key="Slider.SteelThumb" TargetType="Thumb">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Width" Value="15" />
        <Setter Property="Height" Value="15" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Ellipse x:Name="knob"
                             Fill="{DynamicResource Brush.Card}"
                             Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}"
                             StrokeThickness="2"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.Effect>
                            <DropShadowEffect BlurRadius="3" ShadowDepth="1" Direction="270"
                                              Opacity="0.25" Color="#FF000000" />
                        </Ellipse.Effect>
                    </Ellipse>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="knob" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.12" ScaleY="1.12" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="knob" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.2" ScaleY="1.2" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================ -->
    <!--  DEFAULT CYAN SLIDER                                         -->
    <!-- ============================================================ -->
    <Style x:Key="Slider.Steel" TargetType="Slider">
        <Setter Property="MinHeight" Value="18" />
        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}" />
        <Setter Property="Background" Value="{DynamicResource Brush.Slider.Track}" />
        <Setter Property="IsMoveToPointEnabled" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center" Background="Transparent">
                        <!-- base (unfilled) track -->
                        <Border Height="4" CornerRadius="2" VerticalAlignment="Center"
                                Background="{TemplateBinding Background}" />
                        <Track x:Name="PART_Track" VerticalAlignment="Center">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge"
                                              Style="{StaticResource Slider.FillButton}" />
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge"
                                              Style="{StaticResource Slider.EmptyButton}" />
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource Slider.SteelThumb}" />
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================================ -->
    <!--  AXIS SLIDERS  (fill + track tint + ring all from Foreground)-->
    <!-- ============================================================ -->
    <Style x:Key="Slider.Axis" TargetType="Slider">
        <Setter Property="MinHeight" Value="18" />
        <Setter Property="IsMoveToPointEnabled" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center" Background="Transparent">
                        <!-- base track: same hue as the fill, dialed down to a tint -->
                        <Border Height="4" CornerRadius="2" VerticalAlignment="Center" 
                                Background="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Slider}}}" />
                        <Track x:Name="PART_Track" VerticalAlignment="Center">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge"
                                              Style="{StaticResource Slider.AxisFillButton}" />
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge"
                                              Style="{StaticResource Slider.EmptyButton}" />
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource Slider.SteelThumb}" />
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- One Foreground setter colors fill + track + ring per axis -->
    <Style x:Key="Slider.Axis.X" TargetType="Slider" BasedOn="{StaticResource Slider.Axis}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Axis.X}" />
    </Style>
    <Style x:Key="Slider.Axis.Y" TargetType="Slider" BasedOn="{StaticResource Slider.Axis}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Axis.Y}" />
    </Style>
    <Style x:Key="Slider.Axis.Z" TargetType="Slider" BasedOn="{StaticResource Slider.Axis}">
        <Setter Property="Foreground" Value="{DynamicResource Brush.Axis.Z}" />
    </Style>

    <!-- Implicit alias: every bare <Slider> gets the cyan look.
         Axis sliders are opt-in via Style="{StaticResource Slider.Axis.X}".
         Remove this line if you want the default to be opt-in too. -->
    <Style TargetType="Slider" BasedOn="{StaticResource Slider.Steel}" />

</ResourceDictionary>