<UserControl x:Class="Fabolus.Wpf.Features.Export.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fabolus.Wpf.Features.Export"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:ExportViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="240">
    <UserControl.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Font.Sans}" />
            <Setter Property="FontSize" Value="11" />

            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
        </Style>

        <Style x:Key="SegmentButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Muted}" />
            <Setter Property="FontFamily" Value="{StaticResource Font.Sans}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="seg" CornerRadius="5" Background="Transparent" Padding="0,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="seg" Property="Background" Value="{DynamicResource Brush.Card}" />
                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
                                <Setter Property="FontWeight" Value="SemiBold" />
                                <Setter TargetName="seg" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="2" ShadowDepth="1" Direction="270"
                                                          Opacity="0.12" Color="#FF141E28" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource ToolRail}">
        <Grid Margin="16,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" Grid.Row="0">
                <TextBlock Text="FILE FORMAT" Style="{StaticResource FieldLabel}" Margin="0,0,0,7" />
                
                <ComboBox SelectedItem="{Binding FileFormat}" Margin="0,0,0,8" Height="32">
                    <ComboBoxItem Content="STL · triangle mesh" IsSelected="True" />
                </ComboBox>
                
                <Border Background="#FFE6EAED" CornerRadius="7" Padding="3" Margin="0,0,0,24">
                    <UniformGrid Columns="2">
                        <RadioButton Content="Binary" GroupName="FileType"
                                     Style="{StaticResource SegmentButton}"
                                     IsChecked="{Binding IsBinary}" />
                        <RadioButton Content="ASCII" GroupName="FileType"
                                     Style="{StaticResource SegmentButton}" />
                    </UniformGrid>
                </Border>

                <TextBlock Text="DESTINATION" Style="{StaticResource FieldLabel}" Margin="0,0,0,7" />
                
                <Button Command="{Binding BrowseDestinationCommand}" 
                        Background="{DynamicResource Brush.Surface}" 
                        BorderBrush="{DynamicResource Brush.Border}" 
                        BorderThickness="1" 
                        Padding="8"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Path Grid.Column="0" Data="M1.5 3.5v13h17v-10h-7.5l-2-3h-7.5z" Stroke="{DynamicResource Brush.Text.Muted}" StrokeThickness="1.5" Fill="Transparent" Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <TextBlock Grid.Column="1" Text="{Binding DestinationFolder}" TextTrimming="CharacterEllipsis" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" Foreground="{DynamicResource Brush.Text.Primary}" />
                                    <TextBlock Grid.Column="2" Text="Browse" Foreground="#0097A7" FontWeight="Medium" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0" />
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F7F9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>

            <Button Grid.Row="2"
                    Command="{Binding ExportFilesCommand}"
                    Style="{StaticResource Button.Primary}"
                    HorizontalAlignment="Stretch"
                    Height="40">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="{DynamicResource Icon.Export}" Stroke="{DynamicResource Brush.Text.OnAccent}" StrokeThickness="1.8" Fill="Transparent" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                    <TextBlock Text="{Binding ExportButtonText}" VerticalAlignment="Center" FontWeight="SemiBold" />
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</UserControl>
