<UserControl x:Class="Fabolus.Wpf.Features.Export.ExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fabolus.Wpf.Features.Export"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:ExportViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="600" d:DesignWidth="240">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource Font.Sans}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
        </Style>

        <Style x:Key="FormatRadioButton" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" CornerRadius="6" BorderThickness="1" BorderBrush="{DynamicResource Brush.Border}" Background="{DynamicResource Brush.Surface}" Padding="0,10">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock x:Name="header" Text="{Binding Content, RelativeSource={RelativeSource TemplatedParent}}" FontWeight="Bold" FontSize="13" HorizontalAlignment="Center" Margin="0,0,0,2"/>
                                <TextBlock x:Name="sub" Text="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" FontSize="10" Opacity="0.7" FontFamily="{StaticResource Font.Mono}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#17A2B8" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#17A2B8" />
                                <Setter TargetName="header" Property="Foreground" Value="White" />
                                <Setter TargetName="sub" Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="header" Property="Foreground" Value="{DynamicResource Brush.Text.Primary}" />
                                <Setter TargetName="sub" Property="Foreground" Value="{DynamicResource Brush.Text.Muted}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource ToolRail}">
        <Grid Margin="16,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" Grid.Row="0">
                <TextBlock Text="FILE FORMAT" Style="{StaticResource FieldLabel}" Margin="0,0,0,10" />
                
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <RadioButton Grid.Column="0" Content="STL" Tag="triangle mesh" Style="{StaticResource FormatRadioButton}" IsChecked="{Binding IsStlSelected}" />
                    <RadioButton Grid.Column="2" Content="3MF" Tag="package" Style="{StaticResource FormatRadioButton}" IsChecked="{Binding Is3mfSelected}" />
                </Grid>

                <Border BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,20" Background="{DynamicResource Brush.Surface}" Visibility="{Binding Is3mfSelected, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="BAKED OPERATIONS" Style="{StaticResource FieldLabel}" VerticalAlignment="Center"/>
                            <Border Background="#E0F2F1" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="{Binding HasBakedOperations, Converter={StaticResource BoolToVis}}">
                                <TextBlock Text="{Binding BakedOperationsText}" FontSize="10" Foreground="#00796B" FontWeight="Medium"/>
                            </Border>
                        </Grid>
                        
                        <ItemsControl ItemsSource="{Binding BakedOperations}" Margin="0,0,0,10" Visibility="{Binding HasBakedOperations, Converter={StaticResource BoolToVis}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock FontSize="11" Foreground="{DynamicResource Brush.Text.Primary}" LineHeight="18">
                                        <Run Text="• " /><Run Text="{Binding Text}" />
                                    </TextBlock>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                        <TextBlock Text="None" FontSize="11" Foreground="{DynamicResource Brush.Text.Muted}" Margin="8,0,0,10">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasBakedOperations}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        
                        <Border Background="#E0F7FA" CornerRadius="4" Padding="8,6">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                                <Path Data="M6 1C3.24 1 1 3.24 1 6s2.24 5 5 5 5-2.24 5-5-2.24-5-5-5zm.5 7.5h-1v-4h1v4zm0-5h-1v-1h1v1z" Fill="#0097A7" Width="10" Height="10" Stretch="Uniform" Margin="0,2,6,0" VerticalAlignment="Top"/>
                                <TextBlock FontSize="10" Foreground="#006064" VerticalAlignment="Top" TextWrapping="Wrap" Width="160">
                                    The <Run Text="{Binding PrintableMeshName}" FontWeight="SemiBold"/> is the printable item. Other components are included as non-build references.
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

            </StackPanel>

            <Button Grid.Row="2"
                    Command="{Binding ExportFilesCommand}"
                    Style="{StaticResource Button.Primary}"
                    HorizontalAlignment="Stretch"
                    Height="40">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Path Data="{DynamicResource Icon.Export}" Stroke="{DynamicResource Brush.Text.OnAccent}" StrokeThickness="1.8" Fill="Transparent" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
                    <TextBlock Text="{Binding ExportButtonText}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13" />
                </StackPanel>
            </Button>
        </Grid>
    </Border>
</UserControl>
