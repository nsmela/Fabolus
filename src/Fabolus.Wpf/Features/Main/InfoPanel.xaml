<UserControl x:Class="Fabolus.Wpf.Features.Main.InfoPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fabolus.Wpf.Features.Main"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="InfoCard" TargetType="HeaderedContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="HeaderedContentControl">
                        <StackPanel>
                            <ContentPresenter ContentSource="Header" Margin="0,0,0,10" />
                            <ContentPresenter />
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Text.CardHeader" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}" />
        </Style>

        <DataTemplate DataType="{x:Type local:TextInfoItem}">
            <Grid Margin="0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Label}" Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="13" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding Value}" FontFamily="Consolas" FontWeight="SemiBold" Foreground="{DynamicResource Brush.Text.Primary}" FontSize="13" VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:StatusInfoItem}">
            <Grid Margin="0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Label}" Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="13" VerticalAlignment="Center" />
                <Border Grid.Column="1" CornerRadius="4" Padding="6,2">
                    <Border.Background>
                        <SolidColorBrush Color="{Binding Colour}" />
                    </Border.Background>
                    <TextBlock Text="{Binding Text}" FontFamily="Consolas" Foreground="White" FontWeight="SemiBold" FontSize="12" VerticalAlignment="Center" />
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:ProgressInfoItem}">
            <StackPanel Margin="0,6">
                <Grid>
                    <TextBlock Text="{Binding Label}" Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="13" />
                    <TextBlock Text="{Binding Caption}" HorizontalAlignment="Right" Foreground="{DynamicResource Brush.Text.Secondary}" FontSize="11" />
                </Grid>
                <ProgressBar Margin="0,6,0,0" Height="4" Maximum="{Binding Maximum}" Value="{Binding Value}" BorderThickness="0" Background="{DynamicResource Brush.Slider.Track}" Foreground="{DynamicResource Brush.Accent}" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:TitleInfoItem}">
            <TextBlock Text="{Binding Label}" Style="{StaticResource Text.CardHeader}" Margin="0,0,0,10"/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:OutputHeaderInfoItem}">
            <Grid Margin="0,0,0,14">
                <TextBlock Text="{Binding Label}" Style="{StaticResource Text.CardHeader}" VerticalAlignment="Center"/>
                <Border Background="{DynamicResource Brush.Accent.Soft}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="{Binding PillText}" FontSize="10" FontWeight="SemiBold" FontFamily="Consolas" Foreground="{DynamicResource Brush.Accent}"/>
                </Border>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:FileDetailsInfoItem}">
            <Grid Margin="0,4,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Path Data="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8l-6-6zm4 18H6V4h7v5h5v11z" Fill="{DynamicResource Brush.Accent}" Width="12" Height="14" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Grid.Column="1" Text="{Binding FileName}" FontFamily="Consolas" FontSize="13" Foreground="{DynamicResource Brush.Text.Primary}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                <TextBlock Grid.Column="2" Text="{Binding FileSize}" FontFamily="Consolas" FontSize="12" Foreground="{DynamicResource Brush.Text.Secondary}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0,0,0"/>
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:SeparatorInfoItem}">
            <Border Height="1" Background="{DynamicResource Brush.Divider}" Margin="0,4,0,12"/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:SubtitleInfoItem}">
            <TextBlock Text="{Binding Label}" FontSize="11" Foreground="{DynamicResource Brush.Text.Muted}" FontWeight="SemiBold" Margin="0,0,0,10"/>
        </DataTemplate>
    </UserControl.Resources>
    
    <Border HorizontalAlignment="Right" VerticalAlignment="Bottom"
        Margin="0,0,16,16" Width="300"
        Background="{DynamicResource Brush.Card}"
        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1"
        CornerRadius="9" Padding="14,13">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Visibility" Value="Visible" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding InfoItems.Count}" Value="0">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding InfoItems}" Value="{x:Null}">
                        <Setter Property="Visibility" Value="Collapsed" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3"
                      BlurRadius="24" ShadowDepth="8" Direction="270"/>
        </Border.Effect>

        <ItemsControl ItemsSource="{Binding InfoItems}"/>
    </Border>
</UserControl>
