<UserControl x:Class="Fabolus.Wpf.Features.Viewport.ViewportControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:hx="http://helix-toolkit.org/wpf/SharpDX"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <hx:Viewport3DX
            x:Name="MainViewport"
            EnableCursorPosition="True"
            Background="Transparent"
            BackgroundColor="#00000000"
            
            MSAA="Eight"

            ModelUpDirection="0, 0, 1"
            Camera="{Binding Camera, RelativeSource={RelativeSource AncestorType=UserControl}}"
            EffectsManager="{Binding EffectsManager, RelativeSource={RelativeSource AncestorType=UserControl}}"
            UseDefaultGestures="False"
            IsPanEnabled="True"
            IsRotationEnabled="True"
            IsZoomEnabled="True"
            
            ShowCoordinateSystem="True"
            CoordinateSystemHorizontalPosition="-0.92"
            CoordinateSystemVerticalPosition="-0.92"
            CoordinateSystemLabelForeground="#E2E7EA"
            CoordinateSystemLabelX="X" CoordinateSystemLabelY="Y" CoordinateSystemLabelZ="Z"
            
            ShowViewCube="False"
            
            MouseDown3D="MainViewport_MouseDown3D"
            MouseMove="MainViewport_MouseMove"
            MouseUp3D="MainViewport_MouseUp3D"
            KeyDown="MainViewport_KeyDown"
            KeyUp="MainViewport_KeyUp">

            <hx:Viewport3DX.InputBindings>
                <MouseBinding Command="hx:ViewportCommands.Rotate" Gesture="RightClick" />
                <MouseBinding Command="hx:ViewportCommands.Pan"    Gesture="MiddleClick" />
                <KeyBinding   Command="hx:ViewportCommands.ZoomExtents" Gesture="Control+E" />
            </hx:Viewport3DX.InputBindings>

        </hx:Viewport3DX>
    </Grid>
</UserControl>
